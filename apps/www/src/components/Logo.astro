---
type Props = {
  href?: string;
  label?: string;
};

const { href = "/", label = "CortexAI Agent Hub home" } = Astro.props as Props;
---

<a class="brand-lockup" href={href} aria-label={label}>
  <img
    class="brand-lockup__mark"
    src="/brand/cortexai-icon.png"
    width="36"
    height="36"
    alt=""
    aria-hidden="true"
  />
  <span class="brand-lockup__word">CortexAI Agent Hub</span>
</a>
