/** Product palette for Expo. Prefer these over hardcoded CortexAI Agent Hub hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@cortexai-agent-hub/ui-tokens";
